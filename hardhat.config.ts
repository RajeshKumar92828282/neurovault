// Hardhat config stub
// The project has migrated to Stylus/WASM. Keep a lightweight stub
// so IDEs that open this file don't show unresolved import errors.

// Provide a simple type alias for compatibility with editors.
type HardhatUserConfig = any;

const config: HardhatUserConfig = {};

export default config;
