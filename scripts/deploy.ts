console.error('deploy.ts is disabled: project migrated to Stylus/WASM.');
console.error('See contracts/stylus/ and .github/workflows/build_and_release_wasm.yml for the new workflow.');
process.exit(1);
